# Admin Authentication
# Generate with: htpasswd -bnBC 12 "" your-password | tr -d ':\n'
ADMIN_PASSWORD_BCRYPT_HASH=

# JWT Token Security
# Generate with: openssl rand -base64 32
JWT_SIGNING_SECRET_KEY=CHANGE-ME-USE-openssl-rand-base64-32
